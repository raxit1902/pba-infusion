/**
 * API Configuration
 * testMode: If true, returns mock data from frontend. If false, makes real backend calls.
 */
export const API_CONFIG = {
	testMode: true,
	baseURL: 'http://localhost:5000/api',
};
